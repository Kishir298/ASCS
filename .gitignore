.venv/
.venv-macos/
.venv-windows/
.venv-linux/
venv/
env/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# Personal / local secrets and tool state
.env
.env.*
!.env.example
.opencode/
node_modules/

# TLS / device material (never commit even if dropped here by tooling)
*.pem
*.key
*.pfx
*.p12
*.crt
*.cer
.risarms-device.json
*-device.json
device.json

# E2E scratch workspace, if ever created inside the repo
.tmp_workspace/
.aider*

# A.S.C.S. runtime state (regenerated on run)
.ascs/

# Local model scratch outputs (Option C, gitignored, never committed)
ascs_demo_hello.txt
Ollama_tests/

# macOS / Windows / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Coverage / logs
.coverage
htmlcov/
*.log
